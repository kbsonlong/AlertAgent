# 集成测试配置文件
test:
  database:
    host: localhost
    port: 3306
    user: root
    password: password
    name: alertagent_test
    
  redis:
    host: localhost
    port: 6379
    db: 1
    
  timeout:
    default: 30s
    long: 60s
    
  concurrency:
    workers: 3
    max_tasks: 100
    
  plugins:
    dingtalk:
      enabled: true
      timeout: 10s
    email:
      enabled: true
      timeout: 30s
    wechat_work:
      enabled: true
      timeout: 15s